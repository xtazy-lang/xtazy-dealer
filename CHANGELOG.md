# Changelog

## Unreleased

- Added initial `dealer` CLI surface.
- Added project validation for `app.x` / `package.x`.
- Added `.dealer/` and `product/` workflow layout.
- Added local Xtazy toolchain state under `~/.dealer`.
- Added `doctor`, `clean`, `init`, `run`, and local `dealer xtazy` state commands.
